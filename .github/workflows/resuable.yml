name: Reusable Deploy
on: workflow_call #workflow_call is used to be called from other workflows #workflow_dispatch for manual running
jobs: 
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Output Information
        run: echo "Deploying & uploading..."